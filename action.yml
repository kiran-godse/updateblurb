name: Update Blurb 

description: Updatee the Blurb with new response

inputs:
  discussionId:
    description: "The discussion ID of the discussion"
    required: true
  api_key:
    description: "OpenAI API Key"
    required: true
  blurb_body:
    description: "The blurb_body which will be used to update the blurb content"
    required: true

runs:
  using: "node16"
  main: "index.js"
